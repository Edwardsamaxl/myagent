---
description: 多 Agent 协作与本仓库约定（免重复粘贴提示词）
alwaysApply: true
---

# 本仓库协作规则

## 分工（按任务改对应目录，避免一人改全仓）

- **数据 / Ingestion**：`scripts/`、`data/raw/`、`src/agent/core/ingestion.py`、与 `DocumentChunk.metadata` 写入相关的调用链。
- **检索 / 重排**：`src/agent/core/retrieval.py`、`rerank.py`、检索相关配置。
- **生成 / Agent 循环**：`src/agent/core/generation.py`、`agent_loop.py`、`src/agent/application/rag_agent_service.py`、`src/agent/tools/`。
- **接口 / Web**：`src/agent/interfaces/web_app.py`、`main.py`。
- **计划与验收**：`docs/` 下 day 计划、week1 计划；协调任务边界与 API 契约。

## 全局约束

1. 改对外行为（API、chunk 字段、环境变量）前，先看 `docs/day*-*.md` 是否与周计划一致，并在 PR/提交说明里写**验收方式**。
2. 同一文件尽量由单一职责的改动主导；冲突时以 `docs/` 与协调说明为准。
3. 不提交 `.env`、不提交 `data/debug/` 大块输出（见 `.gitignore`）。
4. 接口或契约变更时，同步更新 `data/README.md` 或相关 `docs/`，避免脚本与后端不一致。

## 提示

专职分工的详细说明见 `docs/` 内各 Day 计划；本规则为**项目内**自动生效。若希望**所有仓库**都有通用编码习惯，再在 Cursor **用户设置 → Rules for AI** 里加全局规则。
